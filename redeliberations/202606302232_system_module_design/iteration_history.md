# 迭代历史记录

## 第 1 轮迭代 (M=1) — 2026-06-30

**判定结果**: RETRY

**产出文件**:
- 组件A产出: `a_v1_output_v1.md`
- 组件B诊断: `b_v1_diag_v1.md`
- 组件B质询: `b_v1_challenge_v1.md`
- 判定文件: `iteration_v1.md`

### 需要解决的问题

#### 严重问题（阻断性）

- **C1** 病虫害严重度等级数量与规格书矛盾：设计文档4级 vs 规格书3级。建议确认后统一。
- **C2** alarm_flag 位掩码完全未定义。需从 DATA_INVENTORY.md 引用 0x01~0x80 定义。
- **C3** 告警阈值完全未定义。需补充温度/湿度/光照/CO2/NPK 各传感器阈值。
- **C4** 土壤 NPK 推导公式缺失。需写入 soil_n/soil_p/soil_k 三个推导公式。

#### 一般问题（高优先级——功能性）

- **H1** API 响应缺少 image_path 字段
- **H2** 缺少图片上传/获取 API 端点
- **H3** 缺少热力图功能
- **H4** 缺少环境-病虫害联动分析
- **H5** 设备在线/离线判定未定义
- **H6** 鸿蒙端消息推送机制空白

#### 一般问题（中优先级——完善性）

- **M1** IoTDA 双 Webhook 路由机制未充分说明
- **M2** 数据库表命名与参考文档不一致
- **M3** 决策引擎规则仅有一条示例
- **M4** 缺少数据保留/清理策略
- **M5** OLED I2C 地址缺失
- **M6** 超声波异常值处理未说明

#### 轻微问题（润色性）

L1~L14（14项）：架构命名不一致、命令应答 payload 缺失、NPK 传感器矛盾未标注、防治建议时间窗口缺乏依据、firmware 缺少 tests 目录、docker 镜像 tag 不精确、错误码不完整、历史查询字段不一致、缺少 logging 配置、缺少 WebSocket/SSE 考量、上位机 NPK 表述模糊、缺少 API 版本策略、图表库选型未定、alarm_flag 历史查询缺失。

---

## 第 2 轮迭代 (M=2) — 2026-06-30

**判定结果**: RETRY

**产出文件**:
- 组件A产出: `a_v2_copy_from_v1.md`
- 组件B诊断: `b_v2_diag_v1.md`
- 组件B质询: `b_v2_challenge_v1.md`
- 判定文件: `iteration_v2.md`

### 需要解决的问题

#### 一般问题（中优先级——完善性）

- **N1** 图片存储持久化机制未定义 — 图片上传端点定义了返回路径，但 docker-compose 中无图片存储 volume 挂载，容器重建后历史图片将丢失。建议在 docker-compose 中增加图片存储 volume 挂载（如 `- ./images:/app/images`），或在 §4.7 端点说明中补充存储策略（本地磁盘/外部对象存储）。
- **N2** IoTDA 命令应答 Webhook 回传路径不完整 — 流 B 时序图显示命令应答通过 Webhook 回传 Python API，但 §4.2 仅定义了传感器和AI识别两条 Webhook 规则，缺少命令应答的转发规则。建议方案 A（推荐）：在 IoTDA 增加第三条转发规则 `rule_cmd_response_forward`，将命令应答通过 Webhook 转发至 Python API；方案 B：通过轮询 IoTDA 设备影子获取命令执行结果。

---
